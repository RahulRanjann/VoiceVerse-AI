"""Typed speech-analysis execution boundaries.

Concrete model runtimes are deliberately optional.  The base execution-plane
image can expose the contracts while remaining unable to execute a capability
until an explicitly configured provider is injected.
"""
